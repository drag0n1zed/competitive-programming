#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using vi = vector<int>;
using vll = vector<long long>;

void solve() {
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
#ifdef LOCAL
    freopen(PROJECT_ROOT "/input.txt", "r", stdin);
    // freopen("../output.txt", "w", stdout); // Output to file
#endif
    int t = 1;
    // cin >> t; // Multiple test cases
    while (t--) { solve(); }
    return 0;
}
